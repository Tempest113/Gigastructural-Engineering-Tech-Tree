# pipeline/name_overrides.py: one entry per line, `<technology key> = <display name>  # <justification>`.
# Used only when a technology's resolved localisation value is verbatim its own raw key -- i.e.
# the mod author never actually wrote a display name for it. Every entry below MUST be reviewed
# and justified; this is not a place to guess at names for convenience.

giga_tech_aeternite_weaponry = Aeternite Weaponry Systems  # vendor/mods/gigastructures/localisation/english/giga_ehof_functions_l_english.yml:93 has never been translated by the mod author (value is verbatim the key). Named by pattern-matching this Aeternum tech's two siblings on the same tier ("Aeternite Loop-Quantum Defensive Matrix", "Aeternite Planetcraft Refurbishment") -- found via this session's own screenshot review, not the user's; revisit if Gigastructures ships a real translation upstream.
